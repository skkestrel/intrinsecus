<Window x:Name="SelectionDialogueWindow" x:Class="EhT.Intrinsecus.SelectionDialogue"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Selection" Height="300" Width="203">
	<Grid x:Name="MainGrid">
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Button Content="Squats" Margin="10" Click="Button_Click" Grid.Row="1"/>
		<Button Content="Deadlifts" Margin="10" Click="Button_Click"/>
		<Button Content="Shoulder Press" Margin="10" Click="Button_Click" Grid.Row="2"/>
		<Button Content="Split leg lunges" Margin="10" Click="Button_Click" Grid.Row="3"/>
		<Button Content="Squats" Margin="10" Click="Button_Click" Grid.Row="4"/>


	</Grid>
</Window>
